For this we are using python 3.9.2 or higher and our required libraries can be found inside the requirements.txt file.

simply run: 

pip install -r requirements.txt 

then

python3 main.py

To run this program just run python main.py and it will output all the transformations to the console. To change what you want to transform you can modify the config file.